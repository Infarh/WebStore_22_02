version: '3.4'

services:
  db:
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=yourStrong1_2Password
      - MSSQL_PID=Express

  webstore:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - Logging__Console__FormatterName=Text
      - Database=DockerDB
      - WebAPI=http://api
    ports:
      - "80"
